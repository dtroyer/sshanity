{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "sshanity-build",
      "type": "shell",
      "command": "make",
      "args": ["build"],
      "group": "build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$go"
      ]
    }
  ]
}
